# C-Password-Validation-System
Menu-driven User Management System in C with signup and login features. Supports names with spaces, password validation, masking, and XOR encryption. Prevents duplicate users and allows password updates. Uses file handling to store and manage user data.
